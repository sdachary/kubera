<!DOCTYPE html>
<html class="dark">
<head>
  <title><%= content_for?(:title) ? yield(:title) : "Kubera — Financial Freedom OS" %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta name="theme-color" content="#020617" media="(prefers-color-scheme: dark)">
  <meta name="theme-color" content="#f8fafc" media="(prefers-color-scheme: light)">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-title" content="Kubera">
  <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
  <link rel="manifest" href="/manifest.webmanifest">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
  <%= javascript_importmap_tags %>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@300;400;500;600;700&family=Space+Mono&display=swap" rel="stylesheet">
  <style nonce="<%= content_security_policy_nonce %>">
    :root { --font-sans: 'Outfit', system-ui, sans-serif; --font-mono: 'Space Mono', monospace; }
    body { font-family: var(--font-sans); }
    * { scrollbar-width: thin; scrollbar-color: #1e293b transparent; }
  </style>
</head>
<body class="bg-slate-950 text-slate-100 antialiased min-h-screen">
  <% if current_user %>
    <nav class="border-b border-slate-800">
      <div class="max-w-7xl mx-auto px-4 h-14 flex items-center justify-between">
        <span class="font-semibold tracking-tight">Kubera</span>
        <div class="flex items-center gap-4">
          <span class="text-sm text-slate-400"><%= current_user.email %></span>
          <%= button_to "Logout", "/auth/logout", method: :post, class: "text-sm text-slate-400 hover:text-white transition-colors cursor-pointer bg-transparent border-none" %>
        </div>
      </div>
    </nav>
  <% end %>
  <%= yield %>

  <footer class="border-t border-slate-800 mt-16">
    <div class="max-w-7xl mx-auto px-4 py-6 sm:px-6 lg:px-8 flex flex-col sm:flex-row items-center justify-between gap-4">
      <p class="text-sm text-slate-500">&copy; <%= Time.current.year %> Kubera. Self-hosted.</p>
      <nav class="flex gap-6 text-sm">
        <a href="/privacy" class="text-slate-400 hover:text-white transition-colors">Privacy</a>
        <a href="/terms" class="text-slate-400 hover:text-white transition-colors">Terms</a>
        <a href="/security" class="text-slate-400 hover:text-white transition-colors">Security</a>
        <a href="/dpo" class="text-slate-400 hover:text-white transition-colors">DPO</a>
      </nav>
    </div>
  </footer>
</body>
</html>
